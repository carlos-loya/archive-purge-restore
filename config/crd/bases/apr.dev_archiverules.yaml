---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: archiverules.apr.dev
spec:
  group: apr.dev
  names:
    categories:
    - apr
    kind: ArchiveRule
    listKind: ArchiveRuleList
    plural: archiverules
    shortNames:
    - ar
    singular: archiverule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.table
      name: Table
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .spec.daysOnline
      name: Days-Online
      type: integer
    - jsonPath: .status.lastRunResult
      name: Last-Result
      type: string
    - jsonPath: .status.lastRunRowsArchived
      name: Rows-Archived
      type: integer
    - jsonPath: .status.nextScheduledTime
      name: Next-Run
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ArchiveRule declares a recurring archive job.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ArchiveRuleSpec declares which rows to archive, where to send them, and
              how often to run.
            properties:
              batchSize:
                description: BatchSize overrides the engine's default extract/delete
                  batch size.
                format: int32
                minimum: 1
                type: integer
              databaseRef:
                description: DatabaseRef references a DatabaseConnection in the same
                  namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              dateColumn:
                description: DateColumn is the column used to determine row age.
                minLength: 1
                type: string
              daysOnline:
                description: |-
                  DaysOnline is the number of days a row stays in the source database
                  before becoming eligible for archive.
                format: int32
                minimum: 1
                type: integer
              historyLimit:
                description: |-
                  HistoryLimit is the maximum number of finished Jobs the operator
                  retains for this rule. Older Jobs are deleted on each reconcile.
                format: int32
                minimum: 0
                type: integer
              maxFailures:
                description: |-
                  MaxFailures is the number of consecutive failures after which the
                  operator stops scheduling new runs and marks the rule Ready=False.
                  0 means unbounded — the rule will keep retrying forever. Default 5.
                format: int32
                minimum: 0
                type: integer
              schedule:
                description: |-
                  Schedule is a standard cron expression that controls when archive
                  runs are launched.
                minLength: 1
                type: string
              storageRef:
                description: StorageRef references a StorageBackend in the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              suspend:
                description: |-
                  Suspend pauses scheduling without deleting state. While suspended,
                  already-running Jobs continue to completion but no new firings occur.
                type: boolean
              table:
                description: Table is the source table name.
                minLength: 1
                type: string
            required:
            - databaseRef
            - dateColumn
            - daysOnline
            - schedule
            - storageRef
            - table
            type: object
          status:
            description: |-
              ArchiveRuleStatus reports the observed state of an ArchiveRule including
              the most recent archive run.
            properties:
              activeJobRef:
                description: |-
                  ActiveJobRef points to the currently-running archive Job, if any.
                  At most one Job runs at a time per rule (forbidden concurrency).
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consecutiveFailures:
                description: |-
                  ConsecutiveFailures is the number of finished Jobs in a row whose
                  terminal state was Failed. Resets to zero on the first success. The
                  rule auto-suspends scheduling once this exceeds spec.maxFailures.
                format: int32
                type: integer
              lastJobRef:
                description: |-
                  LastJobRef points to the most recently completed archive Job. It
                  outlives the Job itself if the Job is later GC'd by the history
                  limit.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              lastRunID:
                description: LastRunID is the engine's run ID for the most recent
                  run.
                type: string
              lastRunResult:
                description: LastRunResult is the terminal result of the most recent
                  run.
                enum:
                - Succeeded
                - Failed
                - Running
                type: string
              lastRunRowsArchived:
                description: LastRunRowsArchived is the row count from the most recent
                  run.
                format: int64
                type: integer
              lastRunTime:
                description: LastRunTime is when the most recent archive Job started.
                format: date-time
                type: string
              lastTriggerTime:
                description: |-
                  LastTriggerTime is the most recent value of the
                  `apr.dev/trigger-time` annotation that the reconciler honored. Used
                  to dedupe annotation triggers — the same value will not fire twice.
                format: date-time
                type: string
              nextScheduledTime:
                description: |-
                  NextScheduledTime is when the next run will fire. The reconciler
                  computes this from spec.schedule and uses it to set its requeue
                  delay. While suspended or while a Job is active, this is left
                  unset.
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
